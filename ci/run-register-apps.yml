---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: '8-jdk'

inputs:
- name: scdf-demo

outputs:
- name: run-register-apps-output

run:
  path: pks-earthquakes-demo/ci/run-register-apps.sh